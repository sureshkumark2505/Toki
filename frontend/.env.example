# Toki Frontend Environment Variables
# The frontend connects to the backend API and WebSocket endpoints.
# GEMINI_API_KEY is strictly backend-only for security and is NEVER exposed to the frontend.

VITE_API_BASE_URL=http://localhost:8000
VITE_WS_BASE_URL=ws://localhost:8000
